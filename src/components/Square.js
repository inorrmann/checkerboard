import React from "react";

function Square(props) {
  return (
    <div
      style={{
        backgroundColor: props.color,
        width: '100%',
        height: '100%',
      }}
    />
  )
}

export default Square;